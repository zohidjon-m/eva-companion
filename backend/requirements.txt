# Eva backend dependencies — CORE (text chat, journaling, memory, insights).
#
# Voice (STT/TTS) deps are split out into requirements-voice.txt because Kokoro
# pulls in torch (~2 GB). Install voice separately only when you need speech:
#     pip install -r requirements-voice.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Phase 2 — vault YAML frontmatter (L0 Markdown day files).
PyYAML==6.0.1

# Phase 2 Step B — L2 semantic index (ChromaDB) + local embeddings (fastembed,
# bge-small-en-v1.5). The embedding model is downloaded once out-of-band via
# scripts/download_embed_model.py; runtime is forced offline (see memory/vector.py).
chromadb>=1.5,<2
fastembed>=0.8,<1

# Phase 6 — corpus ingestion. PDF text extraction (pypdf primary, pdfplumber as
# a per-page fallback) and multipart parsing for POST /corpus/upload. All local;
# no network. Markdown/txt need no extra deps.
pypdf>=6,<7
pdfplumber>=0.11,<0.12
python-multipart>=0.0.9

# Phase 1 — model server. Gemma is served by the native llama.cpp `llama-server`
# binary on :11500 (bundled/downloaded by setup). backend/llm/server.py
# launches and supervises it as a subprocess and talks to it over plain HTTP via
# httpx, so no model library (llama-cpp-python) is needed in this venv.

# Voice (STT/TTS) deps live in requirements-voice.txt (faster-whisper + kokoro).
# They pull torch (~2 GB), so they are kept out of the core install. The app
# degrades gracefully without them: voice endpoints report unavailable, text
# chat/journaling/insights are unaffected.

# Test/dev only (Phase 0 socket-guard test uses requests to match the plan).
# httpx is also the runtime client used by backend/llm/client.py to stream.
requests==2.32.3
pytest==8.3.4
httpx==0.28.1
# reportlab is test-only: it mints a real PDF fixture for the Phase 6 loader test
# (which skips itself if reportlab is absent). Not used by the app at runtime.
reportlab>=4,<5
