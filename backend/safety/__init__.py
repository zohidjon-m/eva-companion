"""Safety layer.

Phase 4 ships the interim, deterministic crisis-care check
(:mod:`safety.crisis_check`). It is explicitly a stopgap: the plan replaces it
with NeMo guardrails + the full crisis-care path in a later phase. See the
``CRISIS-SEAM`` marker in ``crisis_check.py``.
"""
