{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Eva's main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
